# Copy to .env for local dev.

# REQUIRED. The app refuses to start without a reachable Postgres.
# Local example (matches the docker command in the README):
DATABASE_URL=postgres://notes:notes@localhost:5432/notes?sslmode=disable

# Port the HTTP server listens on (binds 0.0.0.0).
PORT=8080
